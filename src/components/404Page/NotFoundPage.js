import React from 'react';

const NotFoundPage = () => {
      return (
            <div>
                  <h1>This is page not found</h1>
            </div>
      );
};

export default NotFoundPage;